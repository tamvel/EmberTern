# EmberTern — Project History Archive

This folder holds the **full narrative history** of EmberTern's development: every milestone,
session, bugfix investigation, and design decision, in the words they were originally written
in. It was split out of `CLAUDE.md` during the **Documentation Cleanup Sprint (2026-07-11)**, and
restored by a **second one (2026-08-11)** after `CLAUDE.md` regrew from 472 to 6 849 lines. The goal
both times was to shrink the cost of starting a new Claude Code session without losing any project
knowledge — see `CLAUDE.md` § "Documentation map" for the rationale and the resulting structure.

**Nothing here is loaded automatically at the start of a session.** Read a file only when you
need the backstory on a specific feature or bug — e.g. "why does the metadata reader capture
the command lock exactly once?" or "what did we try before the FlatTree sidebar migration?".
For **current rules and architecture** read `CLAUDE.md`; for **current status** read
`docs/current-state.md` — those are kept current and are what every new session actually needs.

Every file below is a **verbatim extract** — copied byte-for-byte out of the original
`CLAUDE.md`/design-doc text, with only a small provenance header added. No content was
rewritten, summarized, or dropped in the split (see §0 "never lose information"). Gotchas that
appear inline in this narrative are also indexed, thematically and de-duplicated, in
`docs/gotchas.md` — the narrative here is the fuller "how/why we got there" story; the gotchas
file is the fast lookup.

## Files, in chronological order

| File | Covers | Roughly |
|---|---|---|
| [00-v1-definition-of-done-and-backlog.md](00-v1-definition-of-done-and-backlog.md) | V1 DoD checklist + the original post-V1 backlog (mostly stale — later superseded by name) | shipped 2026-05 |
| [01-v1-foundation-and-workspace.md](01-v1-foundation-and-workspace.md) | M1–M6 (scaffold → connections → SQL editor → transactions → metadata sidebar → DDL preview), Explorer Redesign, Custom Titlebar, V1.1 Workspace Persistence, Per-Connection Workspace | 2026-05 |
| [02-sql-editor-and-completion.md](02-sql-editor-and-completion.md) | SQL Editor UX, Saved Queries panel, execute-selected + autoformat, first SQL Autocomplete, formatter lowercase + light-theme highlighting | 2026-05–06 |
| [03-table-detail-core.md](03-table-detail-core.md) | Table Detail tab (Pola/Indeksy/Dane/Zależności core), inline data editing, pagination, smart cell editors | 2026-06 |
| [04-table-structure-editing.md](04-table-structure-editing.md) | `DdlGenerator`, Add/Edit Field dialog, FK Wizard, Field Dependencies panel, Constraint Management, destructive-op confirmation audit | 2026-06-12–13 |
| [05-transactions-settings-connections.md](05-transactions-settings-connections.md) | Transaction TPB hardening (C1/C2), DPAPI-encrypted settings, unified `settings.dat`, transaction-profile lane split, grid layout profiles | 2026-06-13–14 |
| [06-ui-premium-and-stabilization.md](06-ui-premium-and-stabilization.md) | SVG icon system rollout, two stabilization sprints (refresh-storm root cause #1), read-only System Tables | 2026-06-15–18 |
| [07-view-procedure-editors-and-formatter.md](07-view-procedure-editors-and-formatter.md) | View Detail V1, Procedure Detail V1–V1.4, the first PSQL-aware shared formatter, structured Easy Mode | 2026-06-16–20 |
| [08-data-loss-sidebar-and-searchable-combo.md](08-data-loss-sidebar-and-searchable-combo.md) | WorkGuard data-loss protection, UI-state persistence, the sidebar type-ahead → type-to-filter saga, SearchableComboBox (Domain/Column picker) | 2026-06-19–28 |
| [09-object-editors-and-metadata-tree.md](09-object-editors-and-metadata-tree.md) | Function/Generator/Domain/Package/Exception/Index Detail editors, Security Manager, Metadata Tree context menus, the TreeView scroll-jump investigation and the FlatTree sidebar migration that fixed it | 2026-06-29–07-01 |
| [10-grid-execution-performance-metrics.md](10-grid-execution-performance-metrics.md) | SQL Templates drag & drop, Data Grid Filtering & Aggregations, Performance Analysis (Phases 1–6), Execution Metrics module | 2026-07-02–03 |
| [11-activity-monitor-and-session-manager.md](11-activity-monitor-and-session-manager.md) | Database Activity Monitor (Trace) engine + UI + V1.1/V1.2 polish, Session Manager V1 | 2026-07-03–05 |
| [12-search-scripting-export-and-misc.md](12-search-scripting-export-and-misc.md) | Global Search + editor Find/Replace, Firebird routine-reconstruction fixes, Metadata export, Script Executor, Recompile Dependents, Smart SQL Parameters, Export Framework (CSV/XLSX/Clipboard) | 2026-07-05–08 |
| [13-transaction-audit-and-table-designer.md](13-transaction-audit-and-table-designer.md) | The full Transaction Architecture Audit (R1 fixed, R2 open, R3 resolved), the Table Designer buffered-model restore, single-attachment DDL + Developer Mode | 2026-06-17–18 |
| [14-editor-language-frontend-history.md](14-editor-language-frontend-history.md) | The editor rebuild's own etap-by-etap "as-built" record (Etap 0–6 completion notes, UX Polish Phase, post-polish bug-fix sprint) — the history half of `docs/design/editor-architecture.md` | 2026-07-09–11 |
| [15-ux-stabilization-sprint-and-console-refactor.md](15-ux-stabilization-sprint-and-console-refactor.md) | The 11-item UX & Stabilization Sprint, and the transaction/attachment rewrite it turned into: the dedicated **DDL attachment** (WAIT, not co-location — correcting gotcha #122), and the **SQL Editor as a classic Firebird console** (one attachment, one transaction, routing deleted). Also records why `FirebirdScriptExecutor` is known-broken and deferred. **Read before touching transactions, attachments, or the Script Executor.** | 2026-07-14 |
| [16-stage8-smart-editing.md](16-stage8-smart-editing.md) | **Stage 8 — Smart Editing & Structural Assistance.** M1 **Structural Matching** (Related Elements Highlighting, done + finalized). M2 **Smart Snippets** was **built then reverted** — the VS/Rider snippet-session UX was wrong for experienced devs; the code-writing experience was redesigned into **Language Completion + Typing Ergonomics** (see `docs/design/editor-language-expansion.md`). | 2026-07-16 |
| [17-completion-matching-philosophy.md](17-completion-matching-philosophy.md) | **Completion milestone — prefix-first IntelliSense** (separate from Stage 8). Interactive completion becomes a prediction engine, not a substring search: `CompletionEngine` the single authority, pure `CompletionMatcher` owns filtering/ranking, UI a passive view, AvaloniaEdit substring filter disabled. **`CompletionMatcher` DONE**; engine-fold + App passive-view rewrite is the recorded, entangled remaining step (needs interactive visual QA). | 2026-07-16 |
| [18-language-completion.md](18-language-completion.md) | **Language Completion & Typing Ergonomics** — the code-writing experience that replaced Stage 8 M2. Finishes daily Firebird constructs the developer already started typing (Tab + a passive hint, grammar-armed, synchronous), with `begin…end` as a delimiter pair (Typing Ergonomics, not a construct). As-built: revert M2 → Core catalog+resolver → grammar-aware arming → App Tab-expand + hint (done, awaits visual QA). Typing Ergonomics is next. | 2026-07-16 |
| [19-firebird-debugger.md](19-firebird-debugger.md) | **Stage X — Firebird Debugger** arc, as-built diary. **P1 (AST: exception handlers)** — `WHEN … DO` becomes a `WhenHandler` node (one per clause, ordered `WhenCondition` list + body) on `BlockStatement.Handlers`; parser producer + binder consumer, additive, formatter untouched, malformed→`Other` valve. Records the ratified decision-3 refinement (a `WHEN` may list several comma-separated conditions, so the model carries the whole list). | 2026-07-17 |
| [20-stage-q-quick-fixes.md](20-stage-q-quick-fixes.md) | **Stage Q — Quick Fixes & Code Actions (COMPLETE, Q0–Q5).** The first stage that modifies the user's code on its own initiative. Records the three decisions that came from reading the shipped code rather than planning (fixes are computed on demand, not stored on `Diagnostic`; the §0-safe apply idiom was generalized out of rename into ONE `TextEditApplier`; hover stays read-only), the evaluated naming split (`CodeAction` currency vs `QuickFixEngine` producer), the v1 action set and what was refused and why, and the **six-round light-bulb investigation** whose cause was a theme-scoped brush looked up without a theme variant (gotchas #250–#252). | 2026-07-25 |

| [21-data-import.md](21-data-import.md) | **Data Import (etapy I0–I12), the module end to end.** One working surface with collapsible sections — deliberately NOT a wizard; one pipeline for every source (proven three times: XLSX, XLS, clipboard, none of which touched it); `ImportConfiguration` as the single representation of every user decision; the module’s own transaction on its own attachment (I7.5); `CREATE TABLE` on the Ddl lane (#213); named profiles that required no model change (I11 was the design’s own audit). **I12 close-out**: a million rows in 14.0 s at 71 437 rows/s with a FLAT heap, the UI audit in both palettes, and a closing table of what stays OPEN with a reason for each. | 2026-07-26–27 |

| [22-architecture-hardening-sprint.md](22-architecture-hardening-sprint.md) | **Architecture Hardening / Product Safety — an external audit verified, then acted on.** Not a functional sprint: seal real risks only. Records which audit findings were real and which were not (A-02's P0 rating rejected as a ratified design decision; A-04 real as a documentation defect but not as a corruption path; A-08 declined), and the two the audit got *wrong in the safe direction* — A-05's mitigating argument was stale (the Office module now READS untrusted `.xlsx`), and A-01 had a second failure mode needing no concurrency at all (the New-object template is `CREATE OR ALTER`, so a name collision silently overwrites). Delivers the **DDL change-safety gate** (fingerprint + refuse; live-proven 19/19, including the correction that a DDL reconstruction synthesizes a stub and therefore cannot answer "does this exist"), **settings load health** (`Save` refuses over a file it cannot read — a grid-column resize used to destroy connection profiles and passwords), the corrected document-mutation contract + its tripwire test, the patched dependencies, and a status chip that could no longer lie. | 2026-07-27 |
| [23-acceptance-fix-round.md](23-acceptance-fix-round.md) | **Fourteen defects from ordinary use, grouped into SIX causes and closed in one pass** before M3.2d. Read it for how the grouping paid: three reports were not where they pointed (the result grid's column order was a *replayed profile*, not the column builder; the disabled hammer was `Opacity` letting the toolbar through, not a colour — `AccentColor` is identical in both themes; the "tooltip" that only a restart removed is an `OverlayLayer` card). Delivers **cross-process safety for `settings.dat`** (two instances could publish an EMPTY file, which then loaded as `Missing` and made defaults permanent — and the lesson that *a race is the wrong instrument for pinning a race*), the `EXTRACT(YEAR …)` binder position, positional typing for `EXECUTE PROCEDURE` placeholders (**not** a regression — the count gate was original), the `INTO` list on the shared formatter builder, resize-vs-sort on the result header, three M2b/M2c residues, and **`Alt+F`'s return** — where two guards fired in opposite directions without anyone touching a string. Also records a fix that was **deleted for being inert**. | 2026-08-03 |

| [24-stabilization-sprint.md](24-stabilization-sprint.md) | **Sprint stabilizacyjny (S-1…S-6) before M4** — six defects from ordinary use, and **two of them were not what the report described.** Read it for the method: „zgłoszenie mówi, GDZIE użytkownik to zobaczył, nie CO jest zepsute". The domain report („the change does not save") was **worse** than reported — a rule #11 data-loss path where a read discarded `RDB$FIELD_SOURCE` and the next Compile wrote the base type back, destroying the domain link (demonstrated live: the catalog came back holding `RDB$3`). The tooltip report („works on `VariableName`, not on `:VariableName`") had **no colon bug at all** — the variable was the STATEMENT KIND, since an embedded `SELECT` was the one range no binder covered. Also records the three shared causes (an implicit set of editable grids behind two separate reports; a metadata snapshot that could not say „not yet"; two layers discarding information they had), the fix that **changed the debugger as a side effect** — and why its own 38-case fidelity probe said nothing about it (**not one of its 22 routines contains a singleton `SELECT … INTO`**: a measurement can reproduce a mechanism without reproducing the state). | 2026-08-05 |

| [25-grid-consistency-sprint.md](25-grid-consistency-sprint.md) | **Sprint spójności gridów danych, before M4** — dwa zgłoszenia, oba po pomiarze inne niż opisywało zgłoszenie, i **w obu pomiar zmienił ZAKRES, nie tylko diagnozę.** Czytaj dla gotchy **#322**: reguła bezpieczeństwa wypowiedziana o KLASIE rzeczy („siatka danych") może być fałszywa o każdym jej rzeczywistym elemencie — `EditableGridKind` wstrzymywał rolę wysokości z uzasadnieniem brzmiącym jak pomiar, prawdziwym o siatce danych w ogóle i nigdy niesprawdzonym na tej jednej, której dotyczyło (wiersz ma **stałą** `Height="32"`, więc nie ma jak urosnąć) — a **istniejący test pilnował złego zachowania i nazywał poprawkę „the tempting simplification"**. Lekarstwo: pilnować PRZESŁANKI, nie POLITYKI. Plus: „menu Table Data" okazało się brakiem operacji kopiowania w **czterech z pięciu** gridów, więc zakres poszedł do użytkownika zamiast zostać zgadnięty; jeden wspólny `GridCopyText` (12 istniejących testów przeszło bez edycji ani jednego ciągu = dowód bajtowej niezmienności); i **#323** — strażnik, którego klauzula zapasowa odpowiadała „tak" dokładnie dla tego, co miał wykrywać. | 2026-08-07 |

| [26-firebird-language-completeness-sprint.md](26-firebird-language-completeness-sprint.md) | **Sprint zgodności z gramatyką Firebirda + polish, before M4** — użytkownik odmówił kolejnej poprawki „napraw DATEADD, koniec" i zażądał audytu. Czytaj dla gotchy **#324**: *lista dozwolonych wyjątków nigdy nie zbiegnie się na JĘZYKU, bo jej kompletność wyznaczają zgłoszenia, które ją zbudowały* — cztery poprzednie poprawki dodały po jednym predykacie pozycyjnym, a zbieżnym narzędziem jest SŁOWNIK (ograniczony językiem, skończonym i udokumentowanym), którego jedyną konsekwencją jest MILCZENIE. ⚠ Ale słownik sam jest za tępy i **dowiódł tego istniejący strażnik**: `v = year;` to prawdziwa nieznana zmienna, więc regułą jest SĄSIEDZTWO SŁÓW. Korpus 80 konstrukcji z Language Reference: **26 czerwonych przed zmianą**, zgłoszone były podzbiorem. Plus **#325** (bliźniaczy defekt w spacerze zapytaniowym, którego nikt nie zgłosił, bo jego objaw jest cichszy — ciche BŁĘDNE wiązanie zamiast squiggle'a), **#326** (nierozpoznany PREFIKS instrukcji zwija ją w liść kończący się na pierwszym średniku — kształt #301 dalej; naprawa samego parsera zostawia squiggle), **#327** (odmowa oparta na prawdziwej przesłance i niewynikającym wniosku — debugger nie umiał kroczyć kursora z `NEW`, bo „syntetyczne zmienne tam nie istnieją"; kursor ich nigdy nie potrzebował) i ⭐⭐ **#328** (*„sztywny format daty" to była aplikacja wiernie odtwarzająca nadpisanie w Windows samego zgłaszającego* — pomiar odwrócił diagnozę i wynikiem było „tu nie ma defektu"). | 2026-08-07 |

| [27-post-m5-ux-package.md](27-post-m5-ux-package.md) | **Pakiet UX po M5 (w toku)** — sześć zgłoszeń ze zwykłego używania, po zamknięciu Product Polish. ⭐⭐ Czytaj dla wyniku fazy 0: **trzy z sześciu opisywały coś innego, niż się wydawało, i za każdym razem zmieniło to KSZTAŁT pracy** — „biała ikona" była ratyfikowaną decyzją wykonaną w połowie (przycisk przeniesiono na rolę, zakładki nie, przez co była jedyną z sześciu na kolorze rodzaju); „może da się pokolorować plan przez highlighting SQL" trafiło w złe narzędzie, bo **parser planu już istnieje, a widok wyrzuca jego wynik** (`DisplayText => Node.RawText`); a „Live DDL w New Table" to **pięć bajtowo identycznych kopii**. Plus **#347** (wariant nadpisujący `Foreground` na kontrolce i na jawnych dzieciach wciąż przegrywa z setterem szablonu na `ContentPresenter` — i przegrywa TYLKO dla treści będącej stringiem; mechanizm zepsuty w obu motywach, Dark przechodził przypadkiem) wraz z jego drugą połową: **strażnik obejmujący progiem kontrastu stan świadomie przygaszony zaświecił na czerwono na poprawnym produkcie**, czyli #322 popełnione wewnątrz strażnika pisanego przeciwko temu błędowi. I **#348** — brak REJESTRACJI zawodzi tak samo cicho jak brakujący słownik zasobów, a render zrobiony bez niej wygląda całkowicie wiarygodnie. ⭐⭐ Z punktu 4: **parser planu JUŻ istniał, a widok wyrzucał jego wynik**, więc kolorowanie nie potrzebowało nowej gramatyki — plus **#349** (rola tokenu decyduje o obowiązującym progu kontrastu: ikona 3:1, ten sam kolor jako tekst 11 px 4,5:1) i ⛔⛔ **#350**, najważniejsze procesowo: **sprzątanie po odrzuconym eksperymencie skasowało ZAAKCEPTOWANY mechanizm, a git nie miał czego przywrócić — pliki nigdy nie były commitowane**. ⛔ Warianty W1/W2 kolorowania planu są ODRZUCONE i opisane w §6.5 — nie projektować ich ponownie. | 2026-08-09 → |

| [28-localization-core-stage.md](28-localization-core-stage.md) | **Localization — etap Core/Firebird (w toku)** — migracja producentów komunikatów spoza App na seam D‑3. ⭐⭐ Czytaj dla wyniku C0: **odziedziczona liczba „≈280" nie przetrwała pomiaru — trzy z dziesięciu pozycji inwentarza opisywały coś innego, niż twierdziły**, i za każdym razem zmieniało to KSZTAŁT pracy: `CharsetCatalog` nie ma ani jednego opisu (to nazwy charsetów Firebirda), Data Import **był już zbudowany poprawnie** (enum kodu, komentarz mówi wprost *„as a code — never a message"*), a `FirebirdDiagnostics` deklaruje o sobie *„No UI"* i pisze wyłącznie do `%TEMP%`. Realny zakres: **~170–190**. Plus cztery ograniczenia, które przesądzają o kształcie migracji — równość wartościowa `Diagnostic` (`record struct`), **sklejanie zdań** w `ExecutionSummary` (niemożliwe do przetłumaczenia bez zmiany kształtu; polski ma trzy formy mnogie), `ex.Message` jako kanał catch-all na ekran (więc „throw ⇒ techniczny" jest fałszywe w obie strony) i strażnik skanujący tylko assembly Core. ⭐ Odkrycie uboczne: **drugi, ratyfikowany mechanizm na ten sam problem już istniał** (enum kodu → `UiStrings`), stąd reguła „kiedy enum, kiedy `MessageKey`". Z C1: **#353** (rozgłoszenie docierające do POJEMNIKA nie dociera do VM jego treści — a przy jednym języku defekt renderuje się bezbłędnie aż do pierwszego przełączenia) i **#354** (przeniesienie miejsca wywołania na wspólny formater przenosi też decyzję o KULTURZE liczb — ⭐⭐ Z **C6** — najważniejsze uogólnienie całego etapu: sklejanie zdań to **trzy** różne problemy (szyk · konkatenacja fragmentów · kategoria liczby), a mechanizmu wymagał **tylko trzeci** — dwa pierwsze rozwiązuje zwykła reguła D‑3 zastosowana w rozdzielczości ZDANIA. Plus **#362–#365**: angielski szyk potrafi siedzieć w UKŁADZIE (dwa sąsiadujące bindingi), a o formach liczby decyduje JĘZYK, nie producent. | 2026-08-10 → |

| [29-formatter-and-psql-dml-binding-fix.md](29-formatter-and-psql-dml-binding-fix.md) | **Dwa defekty z jednego zgłoszenia użytkownika** („autoformater sobie nie radzi" + „jedna `zasobtechcrp` nie ma koloru"), oba **szersze niż zgłoszenie**. ⭐⭐ Czytaj dla **#359**: *formater, który gubi leksem, nie objawia się jako błąd formatowania* — siatka §0 cofa instrukcję do wersji dosłownej, więc funkcja wygląda na bezczynną, a **każdy test zachowania leksemów jest zielony przez całe życie defektu**, bo cofnięcie do dosłownej zachowuje leksemy idealnie; asercją, która potrafi zapalić, jest *„siatka NIE zadziałała"*. Przyczyna: `EmitSelectQuery` renderuje zapytanie z KLAUZUL, a komentarz materializuje się z triwii tokenu, który poprzedza — więc komentarz między ostatnią klauzulą a tym, co zapytanie ZAMYKA (`;` / `INTO` / `DO`), nie należał do nikogo. ⚠ Pierwsza hipoteza („`EmitQuery` gubi komentarze") została **obalona pomiarem** — komentarz wewnątrz klauzuli zawsze przechodził; trzeba było odczytać wyjście PRZED siatką. ⚠ Drugi powód przeżycia: wspólny korpus **nie miał prawie żadnych komentarzy**. ⭐⭐ I **#360**: *suita spójności zmieniająca JEDEN wymiar przy ustalonym drugim czyta się jako wyczerpująca i nie jest* — `SemanticHighlightConsistencyTests` istnieje dokładnie dla objawu „obiekt kolorowany w FROM, ale nie w UPDATE" i pilnuje wszystkich pięciu rodzajów DML **tylko na top-level**, a wiersze dla ciała procedury pilnują ZAPYTANIA; skrzyżowania nigdy nie było — i tam mieszkał defekt: binder PSQL był **drugą ścieżką**, która wiązała podzapytania i **nigdy nie deklarowała CELU**, więc `update t` wiązał się w skrypcie i **nie wiązał niczego** w procedurze. ⭐ Widoczna połowa to jedna niepokolorowana nazwa; gorsza połowa to KONSEKWENCJA — bez zadeklarowanego aliasu cała instrukcja traci referencje `alias.kolumna`. Plus nota procesowa: pierwszy pełny przebieg dał **13 niepowodzeń, żadne moje** — ręcznie przepisany filtr partycji miał 12 nazw klas headless przy 18 w kodzie. | 2026-08-10 |

| [31-the-constant-rule-and-two-ui-defects.md](31-the-constant-rule-and-two-ui-defects.md) | **THE CONSTANT RULE — lustro defektu z #29 — plus karta, która wypisała własną nazwę typu.** Trzy zgłoszenia z jednej wiadomości; trzecie okazało się **brakiem możliwości w kontrolce firm trzecich**, nie defektem. ⭐⭐ Czytaj dla **#369**: emiter klauzuli, który odtwarza słowo kluczowe ze **STAŁEJ** (`Kw("select")`, `Kw("from")`, `Kw("with")`, operator zbiorowy), nigdy nie renderuje tokenów, które ta stała zastępuje — więc komentarz z ich triwii wiodącej renderuje **nikt**, siatka §0 cofa instrukcję i formater wygląda na **bezczynny**. To dokładne lustro THE TAIL RULE z #29: tam komentarz poprzedzał token, którego żadna klauzula nie **POSIADAŁA**, tu — token, którego żadna nie **RENDERUJE**. ⭐ `EmitProjection` był gorszy niż tracący: indeksował spłaszczoną klauzulę od **0**, więc przy komentarzu wiodącym prawdziwe `select` wpadało do listy KOLUMN i słowo szło **dwukrotnie** — leksem **dodany**, odrzucany przez siatkę równie mocno. ⭐⭐ I ustalenie, które kosztowało drugą iterację: **siatka porównuje KOLEJNOŚĆ**, więc odzyskany komentarz po złej STRONIE tokenu jest tak samo zabójczy jak zgubiony — pierwsze przejście odzyskało wszystkie komentarze i pięć kształtów wciąż było zepsutych przy **równych licznikach**. Zakres zmierzony przed poprawką: **9 zepsutych z 13 kształtów, w czterech emiterach** — zgłoszenie nazwało jeden. Plus **#370**: `x:DataType` na `DataTemplate` jest też typem **DOPASOWANIA**, więc nieaktualny nie daje błędu bindingu — szablon przestaje pasować i host cicho renderuje `ToString()`; ⚠⚠ a przeżyło to dlatego, że oba widoki powoływały się w komentarzu na strażnika `ExecActivityCardTests`, **który nie istniał** — nazwa testu w komentarzu to asercja o repozytorium i nic jej nie sprawdza. §3: **AvaloniaEdit 12.0.0 nie ma żadnego seamu lokalizacyjnego** dla `SearchPanel` (wyeliminowano po kolei: brak klasy `Localization`, brak hooka w API, satelita `pl` niemożliwa przy silnej nazwie, a raportowany napis jest ustawiany z KODU panelu) — nic nie zaimplementowano, decyzja należy do użytkownika (ratyfikowana tego samego dnia: własny panel nad ICH silnikiem — `design/find-replace-panel.md`). §4 i **#371**: **kolumnami procedury selektywnej są jej parametry WYJŚCIOWE** — binder pytał `GetColumns`, co dla procedury jest legalnie puste, więc każde `y.kolumna` po `FROM MY_PROC(:a) y` było nierozwiązane; procedura, która **się kompiluje**, dostawała fałszywe `ET0002`. ⭐⭐ Czytaj dla decyzji o WARSTWIE: sonda pokazała, że completion po `y.` oferowało **zero** pozycji, czyli fałszywy podkreślnik i cicho brakująca funkcja to **ten sam błąd widziany z dwóch stron** — łatka w silniku diagnostyki zazieleniłaby test na zygzaku i zostawiła trzy funkcje zepsute. ⚠⚠ Dwie pułapki w samej poprawce: kuszący sygnał strukturalny (`RoutineTableReference`) jest **zły**, bo Firebird dopuszcza `FROM MY_NOARG_PROC` bez nawiasów, nieodróżnialne od tabeli w TEKŚCIE (katalog wie, tekst nie); oraz brak towarzysza `KnowsRoutineParameters` odtworzyłby S‑2 („na moment wszystko podkreślone") o jeden rodzaj obiektu dalej. | 2026-08-12 |

| [32-audit-followup-2026-08.md](32-audit-followup-2026-08.md) | **Etap audit follow-up — narracja przeniesiona z `docs/current-state.md`, dosłownie.** Izolacja testów (`Loc.LanguageChanged` jest `static`, więc każdy wcześniej zbudowany VM zostawał zapisany — 45 deterministycznych niepowodzeń; ⛔ stary ręczny podział na trzy partycje ukrywał to miesiącami i **nie wraca**), **E** — czytaj-modyfikuj-zapisz na ustawieniach (`Load() ?? new ApplicationSettings()` zamieniał **przejściowy** błąd odczytu w DOMYŚLNE i je zapisywał: **182 nieudane odczyty, 89 zapisów domyślnych, 0 z 5 profili połączeń na końcu** — hasła i profile, po cichu), wyścig sesji headless Avalonii (**przyczyna ustalona i odtworzona deterministycznie, 149/150 dyspozycji pada z czterema wątkami szumu, 0/150 bez** — ⛔ pięć napraw zmierzonych i odrzuconych, to **nie jest** defekt EmberTerna), Faza 4 (ostrzeżenie debuggera o skutkach nieodwracalnych; ⭐ preferencja wycisza **tylko modal, nigdy pasek**), Faza 5 (**strażnik charsetu** — ⚠ „staje się `?`" z audytu było niepełne: **330 znaków staje się innym, prawdopodobnie wyglądającym znakiem**, więc `R = 'Cena £100'` zapisało się jako `R = 'Cena L100'`), Faza 6 (NuGet na najnowsze stabilne; ⭐ paczki uznane za trudne **już były aktualne** — dwie „celowe rozbieżności" to **nie są piny**, tamte wersje nigdy nie powstały) i Faza 7 (`ARCHITECTURE.md` jako stan faktyczny; ⚠ poprzedni plik opisywał `EmberTern.Core` jako **17 plików** przy realnych **304** — czytał się wiarygodnie, opisując inny produkt). | 2026-08-14 → 15, przeniesione 2026-08-15 |
| [33-licensing-l7-key-ceremony.md](33-licensing-l7-key-ceremony.md) | **Licencjonowanie L7 — ceremonia produkcyjnego klucza `R1` i zamknięcie systemu.** Trzy z pięciu punktów §32 okazały się już zbudowane w L4a, więc etap był **ceremonią i jej wykonawcą**. ⭐⭐ `KeyCeremony` liczył odcisk i wpis `TrustedKey` od L2, a `SigningSession.Create` je **wyrzucał** — kroki 5 i 7 procedury §24.1 nie miały wykonawcy w produkcie przez pięć etapów, przy zielonym buildzie (#410). Dalej: `VerifyBackup` bez parametru na klucz oczekiwany · wycofanie strażnika pustej tabeli **w tej samej zmianie**, która dodała klucz (#407) · domknięcie linii §38.6 — prawdziwa licencja `Valid` w `Release` · ⚠ cztery błędy pomiarowe dające **fałszywie negatywny** wynik o materiale klucza (#411) · ⛔ świadome zawężenie: obie kopie sprawdzone na jednej maszynie. ⚠ Procedura żyje osobno w `design/licensing-key-ceremony-runbook.md` — ten plik jej nie duplikuje. | 2026-08-22 |
| [34-licensing-l9-manager-identity.md](34-licensing-l9-manager-identity.md) | **Licencjonowanie L9 — tozsamosc License Managera: wordmark i okno ‚O programie’.** ⭐⭐ Rekonesans obalil dwie przeslanki zlecenia: akcent EmberTerna jest **NIEBIESKI** (`#2D6BBF`, a paleta celowo **nadpisuje** pomaranczowy `SystemAccentColor` Windowsa), wiec „biel + pomarancz’ nie dalo sie zrealizowac przez recykling — oba ciepłe kolory palety **znacza cos** (`TransactionActive` = otwarta transakcja, `Warning` = ostrzezenie); i pasek tytulu produktu **nie ma ani nazwy, ani logo**, bo mark usunieto 2026-08-01 z zapisanym powodem, co samo rozstrzygnelo dwa z trzech pytan (wordmark typograficzny trzyma tamta decyzje, bitmapa by ja odwrocila). ⭐ **Dwie wartosci per motyw sa ZMIERZONE**: wspolna wartosc dala **1,72:1** na jasnej chromie, czyli nazwe produktu, ktorej praktycznie nie ma; obowiazuje 6,81:1 / 4,84:1 przy progu 4,5:1 (14 px SemiBold jest ponizej kazdego wyjatku dla duzego tekstu) — i dlatego test pilnuje, ze wartosci **SIE ROZNIA**. ⚠⚠ **#413**: dwa `TextBlock`i roznych rozmiarow obok siebie rownaja sie **PUDELKIEM, nie linia bazowa**, a `LetterSpacing` jest wlasciwoscia `TextBlock`a, nie `Run`a — wiec „dwa bloki, zeby trackowac osobno’ kupuje przekrzywiony znak za ustawienie, ktorego per-run i tak nie ma. ⭐ `ManagerInfo` duplikuje **MECHANIZM, nigdy wartosc** (oba czytaja atrybuty z jednego `Directory.Build.props`), z trzema odrzuconymi alternatywami zapisanymi przy typie; jedna linia `<Product>` naprawila to, ze EXE twierdzil, ze jego produktem jest EmberTern. ⚠⚠ I **#412**: **trzy z czterech nowych straznikow padly na czytaniu KOMENTARZA** — komentarz, ktory dokumentuje zmiane cytujac **zastapiona** wartosc, stoi z definicji obok nowej, wiec regex bierze proze; naprawione u czytelnika, nigdy przez oslabienie asercji ani przeredagowanie komentarza. Plus nota procesowa: specyfikacje L10 zapisano **przed** L9, bo ratyfikowany projekt istniejacy tylko w transkrypcie nie przezywa granicy sesji. | 2026-08-22 |
| [35-licensing-l10-bulk-send-and-module-closure.md](35-licensing-l10-bulk-send-and-module-closure.md) | **Licencjonowanie L10 — grupowa wysyłka, usuwanie administracyjne i ZAMKNIĘCIE MODUŁU.** ⭐ Nietypowo dla tego projektu: **specyfikacja §60 nie zmieniła kształtu ani razu**, bo jej własny rekonesans zmierzył wszystko z góry — i dlatego każdy defekt, który dotarł do użytkownika, przyszedł stamtąd, gdzie specyfikacja nie patrzyła: z niedotrzymanej obietnicy BCL, ze zwiniętego panelu i ze słowa „usuń”. ⚠⚠ **#414**: `SmtpClient.Timeout` **nie ogranicza** `SendMailAsync` — właściwość ustawiona w L6 *wraz z komentarzem tłumaczącym, po co jest*, a zmierzone **21 078 ms** przy `Timeout = 3 000`; przy implicit TLS na 465 nie ograniczało jej **nic**. ⭐ Ta sama sonda zmierzyła lekarstwo (przeciążenie z `CancellationToken`: 2 995 ms przy tokenie 3 000 ms) — a druga połowa zgłoszenia („ponowne wejście w Ustawienia nie pomagało”) **nie była drugim defektem**, tylko dowodem o CZASIE ŻYCIA obiektu: `finally` istniało i nigdy się nie wykonało. ⭐⭐ Najważniejsza jest §5 — **co znaczy „usuń” w rejestrze append-only**: pięć gałęzi, każda **zmierzona na żywej bazie**, nie wybrana jako polityka; wycofanie to **KOLUMNA `retired_at`, nigdy status** (status opisuje UMOWę, wycofanie — REJESTR); a poprawka do niespójności, którą sama stworzyła poprzednia zmiana, to **DWA liczniki na DWA pytania**: `CountActiveLicenses` mówi, czy operacja jest DOZWOLONA (i tylko o tej liczbie wolno mówić w odmowie), `CountLicenses` — KTÓRA to operacja (widok klucza obcego). ⛔ Oczywista naprawa — „niech licznik ignoruje wycofane” — jest **zła**: kontrola wstępna oddająca porażkę bazie jest gorsza niż jej brak. Plus **K1** i wartość enuma bez producenta (`CompletedWithErrors` — pułapka #233 dokładnie tam, gdzie czytelnik najbardziej musi ufać liście), niezmiennik `Planned == Sent+Failed+Skipped+NotAttempted` czytany **z listy prób**, a nie akumulowany, oraz ⭐ **dwie identyczne ikony to dwie aplikacje nie do odróżnienia** — rozumowanie z L3 było poprawne, a wynik zły, i widać to dopiero, gdy obie stoją otwarte, jedna z kluczem podpisującym. Gotchy **#414–#418**; ⭐ **#416 czytaj dwa razy** — wstrzykiwanie defektu, żeby udowodnić, że strażnik żyje, kłamie na kroku ODTWARZANIA (`os.replace` zachowuje mtime kopii, więc build przyrostowy pomija plik: **trzy pełne przebiegi suity** wydane na „błąd”, który istniał tylko w `obj/`). Stan obowiązujący: `design/licensing-system.md` **§61**. | 2026-08-22 |
| [30-claude-md-current-state-archive.md](30-claude-md-current-state-archive.md) | **`CLAUDE.md` § "Current state" — verbatim archive (2026-07-12 → 2026-08-10).** The status diary as it stood at `00931a2`, moved out by the second Documentation Cleanup Sprint: **5 956 lines / 83 % of `CLAUDE.md`**, 78 entries, nearly all marked `ZAMKNIĘTY`/`CLOSED`/`DONE`. ⭐ Read it as a **dated record**, never as current state — several of its claims were already false when it was archived (the localization branch described as unmerged when it *was* merged; the test count in four layered corrections; a headless-partition figure that had gone stale a third time). ⚠ It is a **second copy**, not the only record: the file's own header maps every stage it describes to the kept-current document that owns it. **For status read `docs/current-state.md`.** | archived 2026-08-11 |
| [handovers/](handovers/) | 🔒 **Closed stage-handover and "next session" prompts** — Product Polish M2a → M5, Localization App + Core. Moved out of `docs/design/` on 2026-08-11, because a "next session" prompt for a finished stage reads as instructions. ⛔ **Do not plan from them**; several premises were refuted by measurement, which is the most valuable thing in them. ⭐ `product-polish-m3-handover.md` still holds rules **R1–R18** and the **21 traps** that future UI work inherits, and `product-polish-m5-next-session.md` holds the already-done measurement for the spacing stage. | 2026-08-11 |

## Where the gotchas went

Every numbered gotcha embedded in this narrative is also collected, de-duplicated, and organized
**thematically** (not chronologically) in [`docs/gotchas.md`](../gotchas.md). That file is the one to
search when you hit a familiar-looking bug — this history archive is for the deeper "why".

⛔ **Do not quote a gotcha count from memory or from prose** — three separate counters have carried it
and all three disagreed while every one was wrong. Measure:
`grep -cE "^[0-9]+\. \*\*" docs/gotchas.md` (2026-08-22, at the licensing module's closure: **407 entry
lines over 405 distinct numbers**, max **#418**; ⚠ numbers **303 and 304 are each used twice**, so a bare "#303" is ambiguous).
