<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:EmberTern.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="EmberTern.App.Views.ChoiceDialog"
        x:DataType="vm:ChoiceDialogViewModel"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        MinWidth="480"
        MaxWidth="720"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

  <Grid RowDefinitions="Auto,*,Auto">

    <Border Grid.Row="0"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1"
            Padding="20,16">
      <TextBlock Text="{Binding Title}" Classes="h1" />
    </Border>

    <!-- ⭐ M4.4: treść komunikatu na rolę Text.Application. Grupa „TextBlock 13 px" z §18.0.5/3 czekała
         na bramę §13.3, która jej nigdy nie podjęła; rozstrzygnięcie jest to samo, które M4.3 podjęło dla
         pustych stanów Session i Trace — przy 13 katalog ma WYŁĄCZNIE rolę kodu, a to jest proza, nie kod.
         ⭐ Rola nazywa dialog jako pierwszego konsumenta we własnym komentarzu w Typography.
         ⚠ Migruje sam rozmiar: interlinii nie czyta żaden z 53 konsumentów tej roli, więc dopisanie jej
         tutaj zmieniłoby odstęp między wierszami zawiniętego komunikatu. -->
    <TextBlock Grid.Row="1"
               Text="{Binding Message}"
               TextWrapping="Wrap"
               FontSize="{DynamicResource Text.Application.Size}"
               Margin="20,18,20,18" />

    <Border Grid.Row="2"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,1,0,0"
            Padding="20,12">
      <!-- WrapPanel (not StackPanel): with several long labels the buttons wrap to a second
           right-aligned row instead of overflowing the fixed-width window and getting clipped. -->
      <ItemsControl ItemsSource="{Binding Options}" HorizontalAlignment="Right">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Right" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="vm:ChoiceOptionViewModel">
            <Button Content="{Binding Label}"
                    Command="{Binding InvokeCommand}"
                    Margin="8,4,0,0"
                    Classes.primary="{Binding IsDefault}"
                    Classes.flat="{Binding IsNotDefault}"
                    IsDefault="{Binding IsDefault}"
                    IsCancel="{Binding IsCancel}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Border>

  </Grid>
</Window>
